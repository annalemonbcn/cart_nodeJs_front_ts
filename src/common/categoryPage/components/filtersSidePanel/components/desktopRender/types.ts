import type { CleanedFiltersData } from '../../types'
import type { FiltersUI } from '@/common/filters/types'

interface DesktopRenderProps {
  uniqueFilters: FiltersUI[]
  cleanedData: CleanedFiltersData
}

export type { DesktopRenderProps }
